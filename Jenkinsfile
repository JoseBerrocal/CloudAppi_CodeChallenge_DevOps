pipeline {

    agent any

    stages {

       stage('Probando Jenkinsfile') {
             steps {
                 echo 'Test en Jenkinsfile'
                 echo 'Test en Jenkinsfile satisfactorio'
                 }
             }

    }
}